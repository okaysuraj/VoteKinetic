import React from 'react';
import { View, Text } from 'react-native';

export const BiometricSetupScreen = (props: any) => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>BiometricSetupScreen</Text>
    </View>
  );
};
