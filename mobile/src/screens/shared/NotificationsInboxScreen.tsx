import React from 'react';
import { View, Text } from 'react-native';

export const NotificationsInboxScreen = (props: any) => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>NotificationsInboxScreen</Text>
    </View>
  );
};
